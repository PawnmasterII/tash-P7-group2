# TASH main repo + audio pipeline dependencies
# Python 3.12 required: webrtcvad-wheels is not available for 3.13+;
# without it the pipeline falls back to an energy-VAD (coarser, still functional).

# Audio pipeline (vendored from TASHaudio)
numpy>=1.24
scipy>=1.10
noisereduce>=3.0.0
vosk>=0.3.45            # offline ASR cue-word spotting — no key, recognises "help"
librosa>=0.10
soundfile>=0.12         # WAV replay I/O

# ML training + inference (agonal detector)
scikit-learn>=1.4       # HistGradientBoostingClassifier, StandardScaler, metrics
joblib>=1.3             # parallel feature extraction + model serialisation

# Optional but strongly recommended: real VAD (energy-VAD fallback if absent)
webrtcvad-wheels>=2.0.10   # Windows/macOS pre-built wheel; use webrtcvad on Linux

# Optional: faster gradient boosting for training (falls back to sklearn HGBC)
# lightgbm>=4.0

# Optional: live microphone capture (only needed for mode="live")
pvrecorder>=1.2
